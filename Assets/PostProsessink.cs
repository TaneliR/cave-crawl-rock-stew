using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering.PostProcessing;

public class PostProsessink : MonoBehaviour
{
    // PostProcessVolume volume;
    // public float bloom = 10f;
    // public float saturation = 5f;
    
    // Bloom            bloomLayer            = null;
    // LenseDistortion lenseDistortLayer = null;
    // ScreenSpaceReflections     spaceReflectLayer     = null;
    // void Start() {
    //     // Create an instance of a vignette
    //     volume = gameObject.GetComponent<PostProcessVolume>();
    //     volume.profile.TryGetSettings(out bloomLayer);
    //     volume.profile.TryGetSettings(out lenseDistortLayer);
    //     volume.profile.TryGetSettings(out spaceReflectLayer);

    // }
    // void Update() {
    //     // Change vignette intensity using a sinus curve
    //     spaceReflectLayer.intensity.value = Mathf.Sin(Time.realtimeSinceStartup);
    // }
    // void OnDestroy() {
    //     RuntimeUtilities.DestroyVolume(volume, true, true);
    // }
}
